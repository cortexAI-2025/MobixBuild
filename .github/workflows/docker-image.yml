name: Docker Image CI

on:
  push:
    branches: [ "claude/android-saas-build-backend-b4p0Z" ]
  pull_request:
    branches: [ "claude/android-saas-build-backend-b4p0Z" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
